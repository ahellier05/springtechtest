SPRING REST API containing two GET request methods:
- Retrieve all users in a database
- Retrieve user who are within a certain mile radius of London or have London as their listed location

Spring Boot application with SWAGGER UI representation using Maven wrapper (3.6.3)

Once the app is running, please navigate to localhost:8080/swagger-ui.html
